# NEXT_SPRINT v0.11 Relationship Model + v0.12 Registration Anchor Steps

Done v0.10.1: anchor :8000 count 1 GE-AERO-GE-AERO-DIP-DISCOVERY after --only-dip cleanup duplicate GEAERO-DISCOVERY vs GEAERO-DIP-DISCOVERY — PWA :8002 title v0.10.1 PWA CORS Fix + Bookmarklet per-record — 5 services :8000 :8001 :8002 :8003 :8004 all CORS — bookmarklet per-record individual for each /Opportunity/006Uj.../view — 16 tags

Current v0.11: Relationship Model — user provided JSON v0.11 2026-09-20 — typed reusable config not free text — primary_identifiers Account Name, project_name, project_id, GDP ID from URL path, Opportunity ID O-xxxxx — secondary sharepoint_urls teams_channels contacts[] — filter_keywords_typed EXACT CONTAINS DOMAIN DATE_RANGE TOKEN_OVERLAP URL_CONTAINS — anti-hallucination DATA_DICTIONARY source of truth Do NOT invent GDP significance — nodes 12 client_master project_card sp_comm_plan sp_risk_log sp_esc gdp_dash gdp_excel emails teams_chats teams_vtt connected raid_agg — edges 17 — path patterns Communications Collaboration Plan docx, Customer Status Report MBR folder, Value Framework pptx, Planning Documents Risk Log xlsx, ESC xlsm O-5552629, Solution Documents site, GDP dashboard 7189 — RAID templates 3 templates columns — GDP columns Engagement Name Account Name GDP ID Project ID GDD GDM PrgM EM/DL Status Date Current Phase Status Indicator Start End Location Summary Practice BU/BSV — no significance — Delta per week — Emails Outlook filters Project IDs client domains contacts keywords SoW PO Contract — aggregate once then delta — PII personal reminders Jane likes coffee Alien_BB peanuts — AWS Comprehend — [Re][Fw] excluded — Teams VTTs video transcripts few channels combination keywords — Connected Chatter free text all posts on Opp ID — RAID aggregation 5 sources LLM assessed multiple cards — noise low GDP/SP high Email/Teams/Chatter — freshness aggregated card level — anchor_id shorter just project name — Project_ReferenceID auto generated UniqueID for multiple IDs

Next v0.12: Registration and anchor steps — build registration fields so you can see what fields need to be added which gives clarity — Project level card contacts identified from collaboration*.docx + initial scan user marks relevant/not + user adds to keep relevant — all scanned/refreshed user inputs — admin-relationship service :8005 for admin adds/maintain/update relationships fields — runtime config data/seed/relationship_model.json — editable diagram artifact — commit to Git Data as Code not DVC — tag v0.11-relationship-model then v0.12-registration-anchor

Test: Copy relationship_model.json to data/seed/ — copy docs — commit — start admin-relationship :8005 python modules/admin-relationship/service.py — GET /model shows 12 nodes 17 edges — GET /config/identifiers primary secondary — GET /config/filters typed enum — PUT /node/{id} update fields — POST /node add new system — PUT /edge add/update relationship with typed condition EXACT CONTAINS DOMAIN DATE_RANGE TOKEN_OVERLAP URL_CONTAINS — no free text