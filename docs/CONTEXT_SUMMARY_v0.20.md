# Context Summary v0.20 Clean HDD Good Harness — to free context avoid hallucinations
Golden master v0.18.0 clean 5cb73d4c2b1f.html 215264 separate collapses Status expand Client 360 stable Edit back pastel simple ALL Clients breadcrumb fix test data dynamic Add new repeatable + empty dashed + Add URL light blue — surgical patches only — no full rewrite — no self-ref notes — no Acme Corp — single toggle — Edit stacked — PRJ ID single line — PII Approve to save changes Private/Team Shared — Relationship Model AWS PII left-sidebar link — Archive enabled justification modal Enable bottom grey — Ask filtering Fuse.js TF-IDF — Edit Save Create new localStorage — Register/Add new link restored — ALL Clients breadcrumb fix useEffect selectedClient — pastel simple exact image_a0eec4.png mint #D6F5E8->#D6E8FF badges 20px
