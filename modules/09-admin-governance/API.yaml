# API Contract — Admin & Governance — PII + Idempotency + Git
openapi: 3.0.0
info:
  title: Admin & Governance — PII + Idempotency + Git
  version: v1.0
paths:
  /09-admin-governance/health:
    get:
      summary: Health check
      responses:
        '200': { description: ok }
  /09-admin-governance/process:
    post:
      summary: Process Admin & Governance — PII + Idempotency + Git
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                anchor_id: { type: string }
                client_name: { type: string }
                project_id: { type: string }
                source_link: { type: string }
                lastModified: { type: string }
      responses:
        '200':
          description: idempotent result
          content:
            application/json:
              schema:
                type: object
                properties:
                  clip_id: { type: string, description: hash(url+lastModified+anchor) }
                  card_id: { type: string }
                  freshness: { type: string, example: 2d ago }
                  significance_score: { type: number, example: 0.9 }
