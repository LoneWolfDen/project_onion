# API Contract — Experience PWA — Pastel UX
openapi: 3.0.0
info:
  title: Experience PWA — Pastel UX
  version: v1.0
paths:
  /08-experience-pwa/health:
    get:
      summary: Health check
      responses:
        '200': { description: ok }
  /08-experience-pwa/process:
    post:
      summary: Process Experience PWA — Pastel UX
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                anchor_id: { type: string }
                client_name: { type: string }
                project_id: { type: string }
                source_link: { type: string }
                lastModified: { type: string }
      responses:
        '200':
          description: idempotent result
          content:
            application/json:
              schema:
                type: object
                properties:
                  clip_id: { type: string, description: hash(url+lastModified+anchor) }
                  card_id: { type: string }
                  freshness: { type: string, example: 2d ago }
                  significance_score: { type: number, example: 0.9 }
